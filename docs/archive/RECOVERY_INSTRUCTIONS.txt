═══════════════════════════════════════════════════════════════
   🚨 ROCm RDNA1 FIX - RECOVERY REQUIRED 🚨
═══════════════════════════════════════════════════════════════

Your system crashed because the kernel patch tried to fake
hardware capabilities. Here's how to fix it:

───────────────────────────────────────────────────────────────
STEP 1: REMOVE THE CRASHING PATCH
───────────────────────────────────────────────────────────────

cd ~/Projects/rocm-patch
sudo ./recovery_script.sh

This removes the patch and rebuilds a clean kernel module.

───────────────────────────────────────────────────────────────
STEP 2: REBOOT
───────────────────────────────────────────────────────────────

sudo reboot

───────────────────────────────────────────────────────────────
STEP 3: CREATE ENVIRONMENT FILE
───────────────────────────────────────────────────────────────

cat > ~/rocm_rdna1_env.sh << 'ENVEOF'
#!/bin/bash
export HSA_OVERRIDE_GFX_VERSION=10.3.0
export MIOPEN_DEBUG_CONV_IMPLICIT_GEMM=0
export MIOPEN_FIND_ENFORCE=3
export MIOPEN_DEBUG_CONV_WINOGRAD=0
export MIOPEN_DEBUG_CONV_DIRECT=0
export MIOPEN_LOG_LEVEL=4
export PYTORCH_ROCM_ARCH=gfx1030
echo "✅ ROCm RDNA1 configured"
ENVEOF

chmod +x ~/rocm_rdna1_env.sh

───────────────────────────────────────────────────────────────
STEP 4: USE IT
───────────────────────────────────────────────────────────────

source ~/rocm_rdna1_env.sh
python3 your_script.py

───────────────────────────────────────────────────────────────
STEP 5: TEST IT WORKS
───────────────────────────────────────────────────────────────

source ~/rocm_rdna1_env.sh
python3 -c "import torch; x=torch.randn(1,3,32,32).cuda(); print(torch.nn.Conv2d(3,16,3,padding=1).cuda()(x).shape)"

Should print: torch.Size([1, 16, 32, 32])

───────────────────────────────────────────────────────────────
WHY THIS WORKS
───────────────────────────────────────────────────────────────

✅ No code changes = no crashes
✅ Uses GEMM fallback algorithms
✅ Works within hardware limits
⚠️  50-100% slower (but stable!)

───────────────────────────────────────────────────────────────
DOCUMENTATION
───────────────────────────────────────────────────────────────

FINAL_STATUS.md          - Complete summary
ENVIRONMENT_TUNING.md    - Detailed config guide
CRASH_ANALYSIS.md        - Why patches failed
recovery_script.sh       - Automated recovery

═══════════════════════════════════════════════════════════════
